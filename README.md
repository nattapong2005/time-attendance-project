# ระบบบันทึกเวลาฝึกงานนักศึกษา (Student Internship Attendance System)

ระบบเว็บแอปพลิเคชันสำหรับจัดการและบันทึกเวลาการเข้าฝึกงานของนักศึกษา รองรับการทำงาน 3 ระดับผู้ใช้งาน ได้แก่ นักศึกษา (Student), อาจารย์ (Teacher), และ ผู้ดูแลระบบ (Admin)

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

### Frontend
- **React.js**: Framework หลักสำหรับการสร้าง User Interface
- **Tailwind CSS**: framework สำหรับจัดหน้าตาและ UI Design
- **Chart.js**: สำหรับแสดงกราฟและสถิติต่างๆ
- **Lucide React**: ชุดไอคอนที่ทันสมัย
- **Axios**: เชื่อมต่อกับ API

### Backend
- **Node.js & Express**: Server และ API Management
- **Prisma ORM**: จัดการฐานข้อมูล (เชื่อมต่อกับ MySQL)
- **JsonWebToken (JWT)**: ระบบยืนยันตัวตน (Authentication)

---

## 🚀 ฟีเจอร์หลัก (Key Features)

### 1. ระบบจัดการผู้ใช้งาน (User Roles)
ระบบแบ่งสิทธิ์การเข้าถึงออกเป็น 3 ระดับ:
- **Admin**: ดูแลจัดการทั้งระบบ, จัดการข้อมูลผู้ใช้, แผนก, สาขา (Saka)
- **Teacher**: ดูภาพรวมการเข้างานของนักศึกษา, อนุมัติการลา, แก้ไขเวลาเข้างานได้
- **Student**: ลงเวลาเข้า-ออกงาน, ดูประวัติการเข้างานของตัวเอง, ยื่นใบลา

### 2. การลงเวลา (Attendance)
- บันทึกเวลา เข้างาน (Check-in) และ ออกงาน (Check-out)
- ตรวจสอบสถานะการเข้างานอัตโนมัติ (เช่น มาสาย, ขาดงาน)
- รองรับการบันทึกสถานะพิกัดหรือสถานที่ฝึกงาน (Internship Location)

### 3. แดชบอร์ดและรายงาน (Dashboard & Reports)
- **สรุปภาพรวม**: จำนวนนักศึกษา, สถิติการเข้างานรายวัน
- **กราฟแนวโน้ม (Trend Chart)**: แสดงจำนวนการ มาปกติ/สาย/ขาดงาน ย้อนหลัง 6 เดือน
- **ตารางสถิติรายบุคคล**: ค้นหาและดูสรุปการขาดลามาสายของนักศึกษาแต่ละคนได้ (พร้อม Pagination)

### 4. การจัดการข้อมูล (Management)
- **User Management**: เพิ่ม/ลบ/แก้ไข ข้อมูลนักศึกษาและอาจารย์
- **Master Data**: จัดการข้อมูลแผนก (Department) และ สาขา (Saka)
- **Leave Management**: ระบบยื่นใบลาและอนุมัติการลา
- **Manual Adjustment**: Admin/Teacher สามารถแก้ไขสถานะการเข้างานย้อนหลังได้กรณีเกิดข้อผิดพลาด

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

- `/frontend`: Source code ส่วนหน้าเว็บ (React)
  - `/src/pages`: หน้าเว็บต่างๆ แบ่งตามสิทธิ์ (Admin, User)
  - `/src/components`: UI Components ที่ใช้ร่วมกัน
- `/backend`: Source code ส่วนเซิร์ฟเวอร์ (Node.js)
  - `/src/controllers`: Logic การทำงานของแต่ละฟีเจอร์
  - `/src/routes`: API Endpoints
  - `/prisma`: Schema ของฐานข้อมูล

## 📦 การติดตั้งและรันระบบ (Installation)

1. **Backend**:
   ```bash
   cd backend
   npm install
   npx prisma generate
   npm run dev
   ```

2. **Frontend**:
   ```bash
   cd frontend
   npm install
   npm run dev
   ```

---
*Created by Antigravity Team*
